{
  "name": "@lumina/contracts",
  "version": "1.0.0",
  "type": "module",
  "description": "LuminaVault: On-chain business savings & reputation for Celo",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:celo": "hardhat run scripts/deploy-vault.ts --network celo",
    "deploy:alfajores": "hardhat run scripts/deploy-vault.ts --network celo-alfajores",
    "verify": "hardhat verify --network celo"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.0",
    "dotenv": "^16.4.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
